file(GLOB_RECURSE srcs ${CMAKE_CURRENT_LIST_DIR}/*.c ${CMAKE_CURRENT_LIST_DIR}/*.cpp)

set(incs ${CMAKE_CURRENT_LIST_DIR})
list(APPEND incs
    ${CMAKE_CURRENT_LIST_DIR}/include
    ${CMAKE_CURRENT_LIST_DIR}/../include
)


component_register(
    COMPONENT_NAME main
    SRCS ${srcs}
    INCLUDE_DIRS ${incs} 
    PRIVATE_REQUIREDS sscma-micro cvi_rtsp ssl crypto hv sophgo
    REQUIREDS  opencv_core opencv_imgcodecs opencv_imgproc
    SRCS "${CMAKE_CURRENT_LIST_DIR}/rtsp_demo.c"
    )